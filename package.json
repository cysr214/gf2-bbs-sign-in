{
  "name": "gf2-bbs-sign-in",
  "version": "1.0.0",
  "private": true,
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun .",
    "build": "bun build --compile --minify --sourcemap --no-compile-autoload-bunfig --bytecode --format=esm --outfile=./dist/gf2-bbs-sign-in ./src/bin.ts",
    "build:win": "bun run build --windows-hide-console"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
